import { durationOfSeconds } from 'aeonvera/helpers/duration-of-seconds';
import { module, test } from 'qunit';

module('Unit | Helper | duration of seconds');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = durationOfSeconds([42]);
  assert.ok(result);
});
