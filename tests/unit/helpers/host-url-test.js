import { hostUrl } from 'aeonvera/helpers/host-url';
import { module, test } from 'qunit';

module('Unit | Helper | host url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hostUrl();
  assert.ok(result);
});
