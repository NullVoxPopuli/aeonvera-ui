
import { resolve } from 'aeonvera/helpers/resolve';
import { module, test } from 'qunit';

module('Unit | Helper | resolve');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resolve([42]);
  assert.ok(result);
});

