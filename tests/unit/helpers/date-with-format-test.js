import { dateWithFormat } from '../../../helpers/date-with-format';
import { module, test } from 'qunit';

module('Unit | Helper | date with format');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateWithFormat(42);
  assert.ok(result);
});
