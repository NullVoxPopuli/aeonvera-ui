.off-canvas-wrap data-offcanvas='true'
  .inner-wrap
    = component navigation

  section.main-section
    .shell
      = outlet

      = outlet 'bottom-footer'
  = outlet 'fixed-footer'

  a.exit-off-canvas
