h4 Restrictions
p.small By default, this discount is allowed on everything. By adding a single constraint, the discount will <em>only</em> work on the selected item(s).

each restraints as |restraint|
  = event/discount/restraint-entry restraint=restraint
br

.row.text-center
  button.tiny click='addNew' Add Restriction
