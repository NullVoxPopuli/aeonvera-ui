{{flat-pickr
  defaultValue=defaultValue
  dateFormat=dateFormat
  timeFormat=timeFormat
  value=selectedDate
  enableTime=enableTime
  altInput='text'
  nocalendar=timeOnly
  onChange=(action (mut selectedDate))
}}
