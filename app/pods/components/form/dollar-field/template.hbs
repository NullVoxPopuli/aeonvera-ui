{{yield}}
{{paper-input
  label=labelText
  icon='attach money'
  value=fieldValue
  type='number'
  min=0
  step='any'
  required=true
  disabled=disabled
  errors=(map-by 'message' fieldErrors)
  placeholder=placeholder
  onChange=(action (mut fieldValue))
}}
