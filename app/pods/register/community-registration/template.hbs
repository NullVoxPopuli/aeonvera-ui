{{#if isAuthenticated}}
  {{to-elsewhere named='left-side-bar'
    send=(component 'app-nav/dashboard-sidebar' title=model.name)}}
{{else}}
  {{to-elsewhere named='left-side-bar'
    send=(component 'app-nav/default-sidebar')}}
{{/if}}

{{outlet}}
